---
alwaysApply: true
---

# コードコメント規約

## C# で記述する

## XML ドキュメントコメント

- public メソッドは必ず `/// <summary>` を付ける。
- private メソッドも public メソッドと同じ粒度で `/// <summary>` を付ける。
- 引数や戻り値の意味が自明でない場合は `/// <param>` と `/// <returns>` も付ける。
- コメントは「何をするか」を簡潔に書き、「なぜそうするか」が重要な箇所は 1 行で補足する。
