<UserControl x:Class="Onta.View.Core.FileEstimatePanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <DockPanel>
    <DataGrid x:Name="EstimateGrid"
              AutoGenerateColumns="False"
              CanUserAddRows="False"
              CanUserDeleteRows="False"
              CanUserResizeRows="False"
              IsReadOnly="True"
              HeadersVisibility="Column"
              GridLinesVisibility="All"
              SelectionMode="Single"
              SelectionUnit="FullRow">
      <DataGrid.Columns>
        <DataGridTextColumn Header="項目"
            Binding="{Binding Name}"
            Width="2*"/>
        <DataGridTextColumn Header="秒数"
            Binding="{Binding Seconds}"
            Width="*"/>
        <DataGridTextColumn Header="サイズ"
            Binding="{Binding SizeText}"
            Width="*"/>
        <DataGridTemplateColumn Header="メーター"
            Width="1.5*">
          <DataGridTemplateColumn.CellTemplate>
            <DataTemplate>
              <ProgressBar Height="14"
                           Margin="2,0"
                           Minimum="0"
                           Maximum="100"
                           HorizontalAlignment="Stretch"
                           VerticalAlignment="Center"
                           Value="{Binding ProgressPercent, Mode=OneWay}"
                           Visibility="{Binding MeterVisibility}"/>
            </DataTemplate>
          </DataGridTemplateColumn.CellTemplate>
        </DataGridTemplateColumn>
      </DataGrid.Columns>
    </DataGrid>
  </DockPanel>
</UserControl>
