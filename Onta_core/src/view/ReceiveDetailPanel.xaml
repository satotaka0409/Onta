<UserControl x:Class="Onta.View.ReceiveDetailPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <Grid Margin="8">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>

    <TextBlock Text="受信詳細"
               FontSize="16"
               FontWeight="SemiBold"
               Margin="0,0,0,12"/>

    <Grid Grid.Row="1">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="Auto"/>
        <ColumnDefinition Width="*"/>
        <ColumnDefinition Width="Auto"/>
        <ColumnDefinition Width="*"/>
        <ColumnDefinition Width="Auto"/>
        <ColumnDefinition Width="*"/>
      </Grid.ColumnDefinitions>
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="Auto"/>
      </Grid.RowDefinitions>

      <TextBlock Text="ファイル名"
                 VerticalAlignment="Center"
                 Margin="0,0,8,8"/>
      <TextBox x:Name="FileNameBox"
               Grid.Column="1"
               Grid.ColumnSpan="5"
               IsReadOnly="True"
               Margin="0,0,0,8"/>

      <TextBlock Grid.Row="1"
                 Text="ファイルサイズ"
                 VerticalAlignment="Center"
                 Margin="0,0,8,0"/>
      <TextBox x:Name="FileSizeBox"
               Grid.Row="1"
               Grid.Column="1"
               IsReadOnly="True"
               Margin="0,0,16,0"/>
      <TextBlock Grid.Row="1"
                 Grid.Column="2"
                 Text="ブロック数"
                 VerticalAlignment="Center"
                 Margin="0,0,8,0"/>
      <TextBox x:Name="BlockCountBox"
               Grid.Row="1"
               Grid.Column="3"
               IsReadOnly="True"/>
    </Grid>
  </Grid>
</UserControl>
