<Application x:Class="Onta.View.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             StartupUri="src/view/MainWindow.xaml">
  <Application.Resources>
    <!-- グレー基調パレット -->
    <Color x:Key="GrayBg">#FF3A3D44</Color>
    <Color x:Key="GrayPanel">#FF4A4E57</Color>
    <Color x:Key="GrayPanelAlt">#FF555965</Color>
    <Color x:Key="GrayBorder">#FF6E7380</Color>
    <Color x:Key="GrayBorderSoft">#FF5C616C</Color>
    <Color x:Key="GrayText">#FFE8EAED</Color>
    <Color x:Key="GrayTextMuted">#FFB0B5BF</Color>
    <Color x:Key="GrayInput">#FF2F3238</Color>
    <Color x:Key="GrayAccent">#FF8A909C</Color>
    <Color x:Key="GrayAccentHover">#FF9AA1AE</Color>
    <Color x:Key="GrayTrack">#FF3F434C</Color>
    <Color x:Key="GrayFillNeg">#FF7E8796</Color>
    <Color x:Key="GrayFillPos">#FFA8A4A0</Color>

    <SolidColorBrush x:Key="BrushBg" Color="{StaticResource GrayBg}"/>
    <SolidColorBrush x:Key="BrushPanel" Color="{StaticResource GrayPanel}"/>
    <SolidColorBrush x:Key="BrushPanelAlt" Color="{StaticResource GrayPanelAlt}"/>
    <SolidColorBrush x:Key="BrushBorder" Color="{StaticResource GrayBorder}"/>
    <SolidColorBrush x:Key="BrushBorderSoft" Color="{StaticResource GrayBorderSoft}"/>
    <SolidColorBrush x:Key="BrushText" Color="{StaticResource GrayText}"/>
    <SolidColorBrush x:Key="BrushTextMuted" Color="{StaticResource GrayTextMuted}"/>
    <SolidColorBrush x:Key="BrushInput" Color="{StaticResource GrayInput}"/>
    <SolidColorBrush x:Key="BrushAccent" Color="{StaticResource GrayAccent}"/>
    <SolidColorBrush x:Key="BrushAccentHover" Color="{StaticResource GrayAccentHover}"/>
    <SolidColorBrush x:Key="BrushTrack" Color="{StaticResource GrayTrack}"/>
    <SolidColorBrush x:Key="BrushFillNeg" Color="{StaticResource GrayFillNeg}"/>
    <SolidColorBrush x:Key="BrushFillPos" Color="{StaticResource GrayFillPos}"/>

    <Style TargetType="Window">
      <Setter Property="Background" Value="{StaticResource BrushBg}"/>
      <Setter Property="Foreground" Value="{StaticResource BrushText}"/>
      <Setter Property="FontFamily" Value="Segoe UI"/>
      <Setter Property="FontSize" Value="12"/>
    </Style>

    <Style TargetType="UserControl">
      <Setter Property="Background" Value="Transparent"/>
      <Setter Property="Foreground" Value="{StaticResource BrushText}"/>
    </Style>

    <Style TargetType="TextBlock">
      <Setter Property="Foreground" Value="{StaticResource BrushText}"/>
    </Style>

    <Style TargetType="Label">
      <Setter Property="Foreground" Value="{StaticResource BrushText}"/>
    </Style>

    <Style TargetType="GroupBox">
      <Setter Property="Foreground" Value="{StaticResource BrushTextMuted}"/>
      <Setter Property="BorderBrush" Value="{StaticResource BrushBorder}"/>
      <Setter Property="Background" Value="{StaticResource BrushPanelAlt}"/>
      <Setter Property="Padding" Value="6"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="GroupBox">
            <Grid>
              <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
              </Grid.RowDefinitions>
              <Border Grid.Row="0" Grid.RowSpan="2"
                      Background="{TemplateBinding Background}"
                      BorderBrush="{TemplateBinding BorderBrush}"
                      BorderThickness="1"
                      CornerRadius="3"
                      Margin="0,8,0,0"/>
              <Border Grid.Row="0" Background="{StaticResource BrushPanel}"
                      Padding="6,0,6,0" Margin="10,0,0,0" HorizontalAlignment="Left">
                <ContentPresenter ContentSource="Header"
                                  RecognizesAccessKey="True"
                                  TextElement.Foreground="{StaticResource BrushTextMuted}"/>
              </Border>
              <ContentPresenter Grid.Row="1" Margin="8,12,8,8"/>
            </Grid>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <Style TargetType="TextBox">
      <Setter Property="Background" Value="{StaticResource BrushInput}"/>
      <Setter Property="Foreground" Value="{StaticResource BrushText}"/>
      <Setter Property="BorderBrush" Value="{StaticResource BrushBorder}"/>
      <Setter Property="CaretBrush" Value="{StaticResource BrushText}"/>
      <Setter Property="Padding" Value="4,3"/>
      <Setter Property="BorderThickness" Value="1"/>
    </Style>

    <Style TargetType="Button">
      <Setter Property="Background" Value="{StaticResource BrushAccent}"/>
      <Setter Property="Foreground" Value="#FF1E2024"/>
      <Setter Property="BorderBrush" Value="{StaticResource BrushBorder}"/>
      <Setter Property="Padding" Value="10,4"/>
      <Setter Property="BorderThickness" Value="1"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Button">
            <Border x:Name="Bd"
                    Background="{TemplateBinding Background}"
                    BorderBrush="{TemplateBinding BorderBrush}"
                    BorderThickness="{TemplateBinding BorderThickness}"
                    CornerRadius="3"
                    Padding="{TemplateBinding Padding}">
              <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="Bd" Property="Background" Value="{StaticResource BrushAccentHover}"/>
              </Trigger>
              <Trigger Property="IsEnabled" Value="False">
                <Setter TargetName="Bd" Property="Opacity" Value="0.45"/>
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <Style TargetType="RadioButton">
      <Setter Property="Foreground" Value="{StaticResource BrushText}"/>
      <Setter Property="Margin" Value="0,2"/>
    </Style>

    <Style TargetType="CheckBox">
      <Setter Property="Foreground" Value="{StaticResource BrushText}"/>
    </Style>

    <Style TargetType="TabControl">
      <Setter Property="Background" Value="{StaticResource BrushPanel}"/>
      <Setter Property="BorderBrush" Value="{StaticResource BrushBorder}"/>
      <Setter Property="Padding" Value="4"/>
    </Style>

    <Style TargetType="TabItem">
      <Setter Property="Foreground" Value="{StaticResource BrushTextMuted}"/>
      <Setter Property="Background" Value="{StaticResource BrushPanelAlt}"/>
      <Setter Property="Padding" Value="12,6"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="TabItem">
            <Border x:Name="Bd"
                    Background="{TemplateBinding Background}"
                    BorderBrush="{StaticResource BrushBorder}"
                    BorderThickness="1,1,1,0"
                    Margin="0,0,2,0"
                    Padding="{TemplateBinding Padding}"
                    CornerRadius="3,3,0,0">
              <ContentPresenter ContentSource="Header" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsSelected" Value="True">
                <Setter TargetName="Bd" Property="Background" Value="{StaticResource BrushAccent}"/>
                <Setter Property="Foreground" Value="#FF1E2024"/>
              </Trigger>
              <Trigger Property="IsSelected" Value="False">
                <Setter Property="Foreground" Value="{StaticResource BrushTextMuted}"/>
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <Style TargetType="DataGrid">
      <Setter Property="Background" Value="{StaticResource BrushInput}"/>
      <Setter Property="Foreground" Value="{StaticResource BrushText}"/>
      <Setter Property="BorderBrush" Value="{StaticResource BrushBorder}"/>
      <Setter Property="RowBackground" Value="{StaticResource BrushInput}"/>
      <Setter Property="AlternatingRowBackground" Value="{StaticResource BrushPanel}"/>
      <Setter Property="HorizontalGridLinesBrush" Value="{StaticResource BrushBorderSoft}"/>
      <Setter Property="VerticalGridLinesBrush" Value="{StaticResource BrushBorderSoft}"/>
      <Setter Property="HeadersVisibility" Value="Column"/>
    </Style>

    <Style TargetType="DataGridColumnHeader">
      <Setter Property="Background" Value="{StaticResource BrushPanelAlt}"/>
      <Setter Property="Foreground" Value="{StaticResource BrushText}"/>
      <Setter Property="BorderBrush" Value="{StaticResource BrushBorder}"/>
      <Setter Property="Padding" Value="8,4"/>
    </Style>

    <Style TargetType="DataGridCell">
      <Setter Property="BorderThickness" Value="0"/>
      <Setter Property="Padding" Value="4,2"/>
      <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
      <Style.Triggers>
        <Trigger Property="IsSelected" Value="True">
          <Setter Property="Background" Value="{StaticResource BrushAccent}"/>
          <Setter Property="Foreground" Value="#FF1E2024"/>
        </Trigger>
      </Style.Triggers>
    </Style>

    <Style TargetType="DataGridRow">
      <Setter Property="Background" Value="Transparent"/>
      <Style.Triggers>
        <Trigger Property="IsSelected" Value="True">
          <Setter Property="Background" Value="{StaticResource BrushAccent}"/>
        </Trigger>
      </Style.Triggers>
    </Style>

    <Style x:Key="PanelBorder" TargetType="Border">
      <Setter Property="Background" Value="{StaticResource BrushPanel}"/>
      <Setter Property="BorderBrush" Value="{StaticResource BrushBorder}"/>
      <Setter Property="BorderThickness" Value="1"/>
      <Setter Property="CornerRadius" Value="4"/>
      <Setter Property="Padding" Value="8"/>
    </Style>
  </Application.Resources>
</Application>
